                .Text = appt.Rooms.Trim();
                .Text = appt.Street.Trim();
                .Text = appt.Number.Trim();
                .Text = appt.HouseNumber.Trim();
                .Text = appt.District.Trim();
                .Text = appt.City.Trim();
                .Text = appt.Floor.Trim();
                .Text = appt.Storeys.Trim();
                .Text = appt.DateCall.Trim();
                .Text = appt.DateFree.Trim();
                .Text = appt.Status.Trim();
                .Text = appt.Deleted.Trim();
                .Text = appt.Firstname.Trim();
                .Text = appt.Secondname.Trim();
                .Text = appt.Surname.Trim();
                .Text = appt.Phones.Trim();
                .Text = appt.Source.Trim();
                .Text = appt.RentTime.Trim();
                .Text = appt.PriceF.Trim();
                .Text = appt.Price.Trim();
                .Text = appt.Auction.Trim();
                .Text = appt.Communal.Trim();
                .Text = appt.Light.Trim();
                .Text = appt.Water.Trim();
                .Text = appt.Pledge.Trim();
                .Text = appt.MarriedTrigger.Trim();
                .Text = appt.MarriedComment.Trim();
                .Text = appt.ChildrenTrigger.Trim();
                .Text = appt.ChildrenComment.Trim();
                .Text = appt.AloneTrigger.Trim();
                .Text = appt.AloneComment.Trim();
                .Text = appt.AnimalsTrigger.Trim();
                .Text = appt.AnimalsComment.Trim();
                .Text = appt.OwnerType.Trim();
                .Text = appt.Farpost.Trim();
                .Text = appt.AgentAgreement.Trim();
                .Text = appt.SuretyAgreement.Trim();
                .Text = appt.Keys.Trim();
                .Text = appt.Nationality.Trim();
                .Text = appt.EmptyTrigger.Trim();
                .Text = appt.EmptyComment.Trim();
                .Text = appt.PartFurnitureTrigger.Trim();
                .Text = appt.PartFurnitureComment.Trim();
                .Text = appt.AreaTrigger.Trim();
                .Text = appt.AreaComment.Trim();
                .Text = appt.BalconyTrigger.Trim();
                .Text = appt.BalconyComment.Trim();
                .Text = appt.LoggiaTrigger.Trim();
                .Text = appt.LoggiaComment.Trim();
                .Text = appt.NewBuildingTrigger.Trim();
                .Text = appt.NewBuildingComment.Trim();
                .Text = appt.ElevatorTrigger.Trim();
                .Text = appt.ElevatorComment.Trim();
                .Text = appt.InternetTrigger.Trim();
                .Text = appt.InternetComment.Trim();
                .Text = appt.RoomsTrigger.Trim();
                .Text = appt.RoomsComment.Trim();
                .Text = appt.LatticesTrigger.Trim();
                .Text = appt.LatticesComment.Trim();
                .Text = appt.PictureToSite.Trim();
                .Text = appt.HotOffer.Trim();
                .Text = appt.NullComission.Trim();
                .Text = appt.Elite.Trim();
                .Text = appt.HotPrice.Trim();
                .Text = appt.HotComission.Trim();
                .Text = appt.HD.Trim();
                .Text = appt.YoutubeLink.Trim();
                .Text = appt.CianLink.Trim();
                .Text = appt.ApptIsFree;